{
  "name": "quote-service",
  "version": "1.0.0",
  "description": "Renders the client-facing Quote approval page shown in Client Hub (web + mobile).",
  "type": "commonjs",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.js"
  },
  "license": "UNLICENSED"
}
